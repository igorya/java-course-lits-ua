CREATE TABLE `User` (
                        `id` int NOT NULL AUTO_INCREMENT,
                        `firstName` text NOT NULL,
                        `lastName` text NOT NULL,
                        `age` int NOT NULL,
                        `address` text,
                        PRIMARY KEY (`id`)
)